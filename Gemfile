source "https://rubygems.org"

# This ensures your local version matches GitHub's version exactly
gem "github-pages", group: :jekyll_plugins

# This is required to run the server on Windows WSL / Linux (Ruby 3.0+)
gem "webrick"